<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="fr.coppernic.scansample">

    <uses-permission android:name="fr.coppernic.permission.BARCODE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/barcode_blue"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/barcode_blue"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="fr.coppernic.scansample.App"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
